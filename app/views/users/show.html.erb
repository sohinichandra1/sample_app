<p>Hello <%= @user.name %></p>
<%= link_to "Share Location", new_user_place_path(@user) %>

<% @user.shared_places.each do |shared_place| %>
  <p>Address: <%= shared_place.place.address %></p>
  <p>Longitude: <%= shared_place.place.longitude %></p>
  <p>Latitude: <%= shared_place.place.latitude %></p>
  <%= image_tag "http://maps.googleapis.com/maps/api/staticmap?center=#{shared_place.place.latitude},#{shared_place.place.longitude}&markers=#{shared_place.place.latitude},#{shared_place.place.longitude}&zoom=2&size=640x400&key=AIzaSyA4BHW3txEdqfxzdTlPwaHsYRSZbfeIcd8",class: 'img-fluid img-rounded', alt: "#{shared_place.place.street_address} on the map"%>
<% end %>

<table class="table">
  <thead>
    <tr>
	    <th>Name</th>
	    <th>Action</th>
    </tr>
  </thead>
  <tbody>
    <% @friends.each do |friend| %>
    <tr>
	    <td><%= link_to friend.name %></td>
	  </tr>
    <% end %>
  </tbody>
</table>
