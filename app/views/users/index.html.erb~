<h1>Users</h1>
<% if signed_in? %>
  <table class="table">
    <thead>
      <tr>
	<th>Name</th>
	<th>Address</th>
	<th>Action</th>
      </tr>
    </thead>
    <tbody>
      <% @users.each do |user| %>
        <tr>
	  <td><%= link_to user.name, user_path(user) %></td>
          <td><%= user.address %></td>
          <td><%= link_to "Edit", edit_user_path(user) if user == current_user %></td>
	 </tr>
         <% end %>
     </tbody>
  </table>
<% end %>
